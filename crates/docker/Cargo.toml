[package]
name = "dockrs-docker"
version = "0.1.0"
edition = "2024"

[dependencies]
bollard = "0.21"
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["sync"] }
tracing = "0.1"
